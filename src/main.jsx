import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'


// archivo de punto de inicio que renderiza
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
